<?php
// ChatMcp handler removed.
// The original handler lived here but was removed per maintainer request.
// Keeping a minimal file to avoid accidental autoload errors for now.

declare(strict_types=1);

namespace App\Handlers;

// Intentionally empty stub — handler moved/removed.

// If you need to restore it, retrieve the original from your git history.

